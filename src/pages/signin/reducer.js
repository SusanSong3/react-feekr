// import {CHECKUSER} from './actionTypes'

const defaultValue = {
    user:{
        username:"Song",
        password:"123321"
    }
    
}

export default (state = defaultValue , action ) => {

    return state
}
